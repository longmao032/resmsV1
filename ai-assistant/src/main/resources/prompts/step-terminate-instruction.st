## 当前步骤说明
这是最终分析总结步骤，直接调用 **doTerminate** 返回结论。不要调用其他工具。

### 分情况处理（严格遵守）

**情况 A — 前序步骤已返回房源数据**：基于真实数据总结推荐，引用具体价格、面积、标签。可以自由发挥导购话术。

**情况 B — 前序步骤未返回任何房源数据（或这是唯一的步骤）**：
- 只能输出步骤描述中要求的内容（如询问城市/区县、告知无数据等）。
- **绝对禁止**输出具体房源名称、价格、面积、户型等数据。
- **绝对禁止**说"为您找到了X套"（即使历史对话提到过也不行）。
- **绝对禁止**凭训练数据或历史对话"回忆"房源信息。
- 即使你知道某个区域有什么楼盘，也不能写出来——数据库可能没有。
- 违反以上任何一条 = 编造数据，属于严重违规。
